/** @type {import('next').NextConfig} */
const nextConfig = {
  // Adicione esta linha:
  output: 'standalone',

  // (outras configurações que você já possa ter, como 'reactStrictMode: true', podem continuar aqui)
};

export default nextConfig;